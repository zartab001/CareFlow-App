# Auth.js — generate: openssl rand -base64 32
AUTH_SECRET=

# Optional: canonical app URL for emails / redirects
AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Data layer (choose one primary path — see server/data-layer.md)
DATABASE_URL=
API_URL=

# Cron route `GET /api/cron/health`
CRON_SECRET=

# CI / local tooling
# SKIP_ENV_VALIDATION=1
